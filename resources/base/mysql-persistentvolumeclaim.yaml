apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  creationTimestamp: null
  labels:
    io.kompose.service: mysql
  name: mysql-pvc
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: mysql
  volumeName: dbserver
  resources:
    requests:
      storage: 5Gi
status: {}
